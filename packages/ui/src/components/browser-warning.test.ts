import { describe, it, expect } from 'vitest'
import { BrowserWarning } from './browser-warning'

describe('BrowserWarning', () => {
  it('exports BrowserWarning component', () => {
    expect(BrowserWarning).toBeDefined()
  })
})
