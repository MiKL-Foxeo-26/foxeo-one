import { describe, it, expect } from 'vitest'
import { OfflineBanner } from './offline-banner'

describe('OfflineBanner', () => {
  it('exports OfflineBanner component', () => {
    expect(OfflineBanner).toBeDefined()
  })
})
